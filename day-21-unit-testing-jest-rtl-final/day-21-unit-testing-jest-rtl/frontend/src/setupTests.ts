/**
 * setupTests.ts — Runs before every test file.
 * Imports @testing-library/jest-dom which adds custom matchers:
 *   toBeInTheDocument(), toHaveTextContent(), toBeDisabled(), etc.
 */
import "@testing-library/jest-dom";
